package com.ouharri.Jobinow.model.enums;

/**
 * Enumeration representing types of authentication tokens.
 * <p>Currently, it includes only the Bearer token type.
 */
public enum TokenType {
    /**
     * Represents the Bearer token type.
     */
    BEARER
}